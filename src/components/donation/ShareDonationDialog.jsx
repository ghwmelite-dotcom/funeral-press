// STUB — separate later task will build the real share dialog. DonatePanel
// renders this inline next to the Donate button. For now render nothing so
// the layout is preserved without a placeholder UI element.
export function ShareDonationDialog(/* { memorial } */) {
  return null
}
