// STUB — full implementation in Task 38. DonatePanel only mounts this when
// wall.donations exists, so in Task 37 tests it never renders. This stub
// exists solely so the import resolves; replace in Task 38.
export function DonorWall(/* { memorialId } */) {
  return null
}
